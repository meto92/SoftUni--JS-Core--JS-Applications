<section id="welcome">
    <div class="container">
        {{> header}}
        {{> navigation}}

        {{#if isAuth}}
            {{> contactPage}}
        {{else}}
            {{> loginForm}}
        {{/if}}

        {{> footer}}
    </div>
</section>