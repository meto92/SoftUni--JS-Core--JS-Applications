{{> header}}

<div id="car-listings">
    <h1>Car Listings</h1>

    <div id="listings">
        {{#each allListings}}
            {{> listing}}
        {{else}}
            <p class="no-cars">No cars in database.</p>
        {{/each}}
    </div>
</div>

{{> footer}}